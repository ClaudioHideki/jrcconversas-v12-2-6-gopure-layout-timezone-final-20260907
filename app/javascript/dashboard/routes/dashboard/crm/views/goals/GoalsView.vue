<script setup>
import { computed, onMounted, reactive, ref } from 'vue';
import { goalsAPI } from 'dashboard/api/crm/commercialCycle';
import productsAPI from 'dashboard/api/crm/products';
import AgentsAPI from 'dashboard/api/agents';
import { useAlert } from 'dashboard/composables';

const rows=ref([]), agents=ref([]), products=ref([]), dashboard=ref({}), loading=ref(false), saving=ref(false), view=ref('dashboard'), step=ref(1);
const dashboardTab=ref('overview');
const selectedGoalId=ref('');
const selectedGoal=computed(()=>dashboard.value.goals?.[0]);
const metricLabel={revenue:'Receita inicial',mrr:'MRR mensal',ticket:'Ticket médio',quantity:'Quantidade',customers:'Clientes',renewals:'Renovações',conversion:'Conversão (%)'};
const methodLabel={approved_orders:'Pedidos aprovados',completed_orders:'Pedidos concluídos',invoiced_orders:'Pedidos faturados',won_deals:'Negócios ganhos'};
const statusLabel={approved:'Aprovado',separating:'Em separação',invoiced:'Faturado',shipped:'Enviado',completed:'Concluído',pending:'Pendente',draft:'Rascunho',canceled:'Cancelado'};
const criteriaText=computed(()=>{const g=selectedGoal.value;if(!g)return '';const c=g.criteria||{};return [metricLabel[g.metric],methodLabel[g.calculation_method]||methodLabel.approved_orders,`${g.period_start} a ${g.period_end}`,c.user_id?agents.value.find(a=>a.id===c.user_id)?.name:'Todos os responsáveis',(c.order_statuses||[]).map(x=>statusLabel[x]||x).join(', ')].filter(Boolean).join(' · ');});
const dashboardTabs=[['overview','Visão geral'],['sellers','Por vendedor'],['products','Por produto'],['actions','Próximas ações']];
const now=new Date(); const iso=d=>d.toISOString().slice(0,10); const monthStart=iso(new Date(now.getFullYear(),now.getMonth(),1)); const monthEnd=iso(new Date(now.getFullYear(),now.getMonth()+1,0));
const form=reactive({name:`Meta de Receita ${String(now.getMonth()+1).padStart(2,'0')}/${now.getFullYear()}`,description:'',metric:'revenue',period_kind:'monthly',period_start:monthStart,period_end:monthEnd,scope_kind:'team',target:'500000',calculation_method:'approved_orders',status:'draft',allocations:[],product_targets:[],indicators:[{name:'MRR (Receita Recorrente)',kind:'financial',metric:'mrr',target:'150000',weight:30},{name:'Novos clientes',kind:'quantity',metric:'customers',target:'20',weight:20},{name:'Renovações de contratos',kind:'quantity',metric:'renewals',target:'10',weight:10}],settings:{include_subteams:true,compare_previous:true,reference_date:'approval',include_cancellations:true,notify:true,mandatory:true,rhythm:'linear'}});
const cents=v=>{
  if(v===null||v===undefined||v==='') return 0;
  if(typeof v==='number') return Math.round(v*100);
  let raw=String(v).trim().replace(/\s/g,'');
  if(raw.includes(',')&&raw.includes('.')) raw=raw.replace(/\./g,'').replace(',','.');
  else if(raw.includes(',')) raw=raw.replace(',','.');
  else if((raw.match(/\./g)||[]).length>1) raw=raw.replace(/\./g,'');
  const parsed=Number(raw);
  return Math.round((Number.isFinite(parsed)?parsed:0)*100);
}; const money=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format((Number(v)||0)/100); const pct=(v,t)=>t?Math.round(v/t*100):0;
const metricValue=value=>['revenue','mrr','ticket'].includes(dashboard.value.metric||'revenue')?money(value):new Intl.NumberFormat('pt-BR',{maximumFractionDigits:3}).format(Number(value)||0);
const monetaryMetrics=['revenue','mrr','ticket']; const isMoneyMetric=computed(()=>monetaryMetrics.includes(form.metric));
const totalTarget=computed(()=>isMoneyMetric.value?cents(form.target):Number(form.target||0)); const allocationTotal=computed(()=>form.allocations.reduce((s,a)=>s+cents(a.target),0)); const productTotal=computed(()=>form.product_targets.reduce((s,a)=>s+cents(a.target),0));
const progressStyle=(v,t)=>({width:`${Math.min(100,pct(v,t))}%`});
const evolutionMax=computed(()=>Math.max(Number(dashboard.value.target_cents)||0,...(dashboard.value.evolution||[]).map(item=>Number(item.realized_cents)||0),1));
const evolutionStyle=value=>({height:`${Math.max(2,Math.round((Number(value)||0)/evolutionMax.value*100))}%`});
const attainment=computed(()=>Math.max(0,Math.min(100,Number(dashboard.value.attainment)||0)));
const attainmentDonut=computed(()=>({background:`conic-gradient(#2563eb 0 ${attainment.value}%, #dbeafe ${attainment.value}% 100%)`}));
const statusSummary=computed(()=>{
  const list=dashboard.value.goals||[];
  return {active:list.filter(g=>g.status==='active').length,draft:list.filter(g=>g.status==='draft').length,completed:list.filter(g=>g.status==='completed').length};
});
const nextActions=computed(()=>{
  const result=[];
  const gap=Number(dashboard.value.gap_cents)||0;
  const forecast=Number(dashboard.value.forecast_cents)||0;
  const target=Number(dashboard.value.target_cents)||0;
  if(gap>0) result.push({title:'Fechar o gap da meta',detail:`Faltam ${metricValue(gap)} para atingir a meta.`,icon:'i-lucide-target'});
  if(target&&forecast<target) result.push({title:'Aumentar cobertura de pipeline',detail:'O forecast atual está abaixo da meta. Priorize oportunidades com maior probabilidade.',icon:'i-lucide-chart-no-axes-combined'});
  if((dashboard.value.ranking||[]).length) result.push({title:'Acompanhar vendedores abaixo do ritmo',detail:'Use o ranking para orientar coaching e redistribuição de oportunidades.',icon:'i-lucide-users'});
  if(!result.length) result.push({title:'Meta sob controle',detail:'Nenhuma ação crítica identificada para o período.',icon:'i-lucide-circle-check'});
  return result;
});
function seedAllocations(){if(form.allocations.length)return; const each=agents.value.length?Number(form.target||0)/agents.value.length:0; form.allocations=agents.value.slice(0,8).map(a=>({user_id:a.id,name:a.name||a.email,target:each.toFixed(2),weight:100}));}
function seedProducts(){if(form.product_targets.length)return; const chosen=products.value.slice(0,6); const each=chosen.length?Number(form.target||0)/chosen.length:0; form.product_targets=chosen.map(p=>({product_id:p.id,name:p.name,target:each.toFixed(2)}));}
function splitMoneyExactly(count){const total=cents(form.target);if(!count)return[];const base=Math.floor(total/count);let remainder=total-base*count;return Array.from({length:count},()=>{const value=base+(remainder>0?1:0);if(remainder>0)remainder-=1;return (value/100).toFixed(2);});}
function distributeEqually(){seedAllocations(); if(!isMoneyMetric.value)return; const values=splitMoneyExactly(form.allocations.length);form.allocations.forEach((a,index)=>a.target=values[index]||'0.00');}
function distributeProducts(){seedProducts(); if(!isMoneyMetric.value)return; const values=splitMoneyExactly(form.product_targets.length);form.product_targets.forEach((a,index)=>a.target=values[index]||'0.00');}
async function loadDashboard(){const goal=rows.value.find(g=>String(g.id)===String(selectedGoalId.value));const {data}=await goalsAPI.dashboard({goal_id:goal?.id,start_date:goal?.period_start||monthStart,end_date:goal?.period_end||monthEnd});dashboard.value=data||{};}
async function load(){
  loading.value=true;
  try {
    const [g,a,p]=await Promise.all([goalsAPI.list(),AgentsAPI.get(),productsAPI.list({active:true})]);
    rows.value=g.data||[];agents.value=a.data||[];products.value=p.data||[];
    if(!rows.value.some(goal=>String(goal.id)===String(selectedGoalId.value))) selectedGoalId.value=(rows.value.find(goal=>goal.status==='active')||rows.value[0])?.id||'';
    await loadDashboard();
  }finally{loading.value=false;}
}

function newGoal(){view.value='wizard';step.value=1;seedAllocations();seedProducts();}
function payload(status=form.status){return {goal:{name:form.name,description:form.description,metric:form.metric,period_kind:form.period_kind,period_start:form.period_start,period_end:form.period_end,scope_kind:form.scope_kind,target_cents:isMoneyMetric.value?cents(form.target):0,target_quantity:isMoneyMetric.value?null:Number(form.target||0),calculation_method:form.calculation_method,currency:'BRL',status,allocations:form.allocations.map(a=>({user_id:a.user_id,target_cents:cents(a.target),weight:Number(a.weight||0)})),product_targets:form.product_targets.map(a=>({product_id:a.product_id,target_cents:cents(a.target)})),indicators:form.indicators.map(i=>({...i,target_cents:i.kind==='financial'?cents(i.target):undefined,target_quantity:i.kind!=='financial'?Number(i.target):undefined})),settings:form.settings}};}
async function save(status='draft'){if(status==='active'&&isMoneyMetric.value&&form.allocations.length&&allocationTotal.value!==cents(form.target)){useAlert('A distribuição por responsáveis precisa fechar exatamente o total da meta.');step.value=2;return;}if(status==='active'&&isMoneyMetric.value&&form.product_targets.length&&productTotal.value!==cents(form.target)){useAlert('A distribuição por produtos precisa fechar exatamente o total da meta.');step.value=4;return;}saving.value=true;try{await goalsAPI.create(payload(status));useAlert(status==='active'?'Meta publicada.':'Meta salva como rascunho.');view.value='dashboard';await load();}catch(e){useAlert(e.response?.data?.errors?.join(', ')||e.response?.data?.message||'Não foi possível salvar a meta.');}finally{saving.value=false;}}
onMounted(load);
</script>

<template>
<div class="h-full overflow-auto bg-n-surface-1 p-4 sm:p-6">
  <template v-if="view==='dashboard'">
    <header class="mb-5 flex flex-wrap items-center justify-between gap-3"><div class="flex items-center gap-3"><span class="grid size-12 place-content-center rounded-2xl bg-blue-600 text-white"><i class="i-lucide-target size-6"/></span><div><h2 class="text-2xl font-bold">Metas</h2><p class="text-sm text-n-slate-9">Acompanhe o desempenho, analise resultados e gerencie metas comerciais.</p></div></div><div class="flex gap-2"><button class="rounded-xl border border-n-weak bg-white px-4 py-2.5 text-sm font-semibold" :disabled="loading" @click="load">Atualizar</button><button class="rounded-xl bg-n-brand px-4 py-2.5 text-sm font-semibold text-white"  :disabled="loading" @click="newGoal">+ Nova meta</button></div></header>
    <div class="mb-4 rounded-xl border border-n-weak bg-n-solid-2 p-3"><label class="text-sm font-semibold">Meta acompanhada<select v-model="selectedGoalId" class="ml-3 rounded-lg border border-n-weak bg-n-solid-2 p-2" @change="loadDashboard"><option v-for="goal in rows" :key="goal.id" :value="goal.id">{{goal.name}} · {{metricLabel[goal.metric]}}</option></select></label><p class="mt-2 text-sm text-n-slate-11">{{criteriaText}}</p><p v-if="!selectedGoal" class="mt-2 text-sm">Nenhuma meta disponível neste período.</p></div>
    <nav class="mb-4 flex flex-wrap gap-2 rounded-2xl border border-n-weak bg-white p-2">
      <button v-for="item in dashboardTabs" :key="item[0]" class="rounded-xl px-4 py-2 text-sm font-semibold transition" :class="dashboardTab===item[0]?'bg-blue-600 text-white shadow':'text-slate-600 hover:bg-slate-50'" @click="dashboardTab=item[0]">{{item[1]}}</button>
    </nav>
    <section class="grid gap-3 md:grid-cols-5"><article v-for="card in [{l:'Meta total',v:metricValue(dashboard.target_cents)},{l:'Realizado',v:metricValue(dashboard.realized_cents)},{l:'Pipeline (em negociação)',v:dashboard.metric==='revenue'?metricValue(dashboard.pipeline_cents):'—'},{l:'Forecast',v:metricValue(dashboard.forecast_cents)},{l:'Gap da meta',v:metricValue(dashboard.gap_cents)}]" :key="card.l" class="rounded-2xl border border-n-weak bg-white p-4 shadow-sm"><p class="text-xs text-n-slate-9">{{card.l}}</p><strong class="mt-2 block text-xl">{{card.v}}</strong></article></section>
    <section v-if="dashboardTab==='overview'" class="mt-4 grid gap-4 xl:grid-cols-3"><article class="rounded-2xl border border-n-weak bg-white p-5 xl:col-span-2"><div class="flex justify-between"><h3 class="font-bold">Evolução da meta</h3><b>{{dashboard.attainment||0}}% atingido</b></div><div class="mt-5 h-56 rounded-xl bg-n-alpha-1 p-5"><div v-if="(dashboard.evolution||[]).length" class="flex h-full items-end gap-1"><div v-for="point in dashboard.evolution" :key="point.date" class="group relative h-full flex-1"><div class="absolute inset-x-0 bottom-0 min-h-1 rounded-t bg-blue-600" :style="evolutionStyle(point.realized_cents)"/><span class="absolute bottom-full left-1/2 hidden -translate-x-1/2 whitespace-nowrap rounded bg-slate-900 px-2 py-1 text-xs text-white group-hover:block">{{point.date}} · {{metricValue(point.realized_cents)}}</span></div></div><p v-else class="grid h-full place-content-center text-sm text-n-slate-9">Sem vendas válidas no período.</p></div></article><article class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Status das metas</h3><div class="mt-8 grid place-content-center"><div class="relative size-40 rounded-full" :style="attainmentDonut"><div class="absolute inset-5 grid place-content-center rounded-full bg-white text-center"><strong class="text-3xl">{{attainment}}%</strong><span class="text-xs text-n-slate-9">atingido</span></div></div></div><div class="mt-5 grid grid-cols-3 gap-2 text-center text-xs"><div class="rounded-lg bg-emerald-50 p-2"><b class="block text-emerald-700">{{statusSummary.active}}</b>Ativas</div><div class="rounded-lg bg-amber-50 p-2"><b class="block text-amber-700">{{statusSummary.draft}}</b>Rascunhos</div><div class="rounded-lg bg-slate-100 p-2"><b class="block text-slate-700">{{statusSummary.completed}}</b>Concluídas</div></div></article></section>
    <section v-if="dashboardTab==='sellers'" class="mt-4 rounded-2xl border border-n-weak bg-white p-5">
      <div class="mb-4 flex items-center justify-between"><div><h3 class="font-bold">Ranking de desempenho</h3><p class="text-sm text-n-slate-9">Meta, realizado e atingimento por vendedor.</p></div><i class="i-lucide-trophy size-5 text-amber-500"/></div>
      <table class="w-full text-sm"><thead><tr class="text-left text-xs text-n-slate-9"><th>#</th><th>Vendedor</th><th>Meta</th><th>Realizado</th><th>Forecast</th><th>%</th></tr></thead><tbody><tr v-for="(r,i) in dashboard.ranking||[]" :key="r.user_id" class="border-t border-n-weak"><td class="py-3">{{i+1}}</td><td class="font-semibold">{{r.name}}</td><td>{{metricValue(r.target_cents)}}</td><td>{{metricValue(r.realized_cents)}}</td><td>{{metricValue(r.forecast_cents||0)}}</td><td><b>{{r.percent}}%</b></td></tr></tbody></table>
      <p v-if="!(dashboard.ranking||[]).length" class="py-8 text-center text-sm text-n-slate-9">Publique uma meta distribuída entre vendedores para formar o ranking.</p>
    </section>
    <section v-if="dashboardTab==='products'" class="mt-4 grid gap-4 xl:grid-cols-2">
      <article class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="mb-3 font-bold">Metas por produto</h3><div v-for="p in dashboard.products||[]" :key="p.product_id" class="mb-4"><div class="flex justify-between text-sm"><b>{{p.name}}</b><span>{{p.percent}}%</span></div><div class="mt-1 h-3 rounded-full bg-n-alpha-3"><div class="h-full rounded-full bg-emerald-500" :style="progressStyle(p.realized_cents,p.target_cents)"/></div><div class="mt-1 flex justify-between text-xs text-n-slate-9"><span>{{money(p.realized_cents)}}</span><span>{{money(p.target_cents)}}</span></div></div><p v-if="!(dashboard.products||[]).length" class="py-8 text-center text-sm text-n-slate-9">As metas por produto aparecerão após a configuração.</p></article>
      <article class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Meta x realizado x forecast</h3><div class="mt-5 space-y-5"><div><div class="flex justify-between text-sm"><span>Realizado</span><b>{{metricValue(dashboard.realized_cents)}}</b></div><div class="mt-2 h-3 rounded-full bg-slate-100"><div class="h-full rounded-full bg-blue-600" :style="{width:`${Math.min(100,pct(dashboard.realized_cents,dashboard.target_cents))}%`}"/></div></div><div><div class="flex justify-between text-sm"><span>Forecast</span><b>{{metricValue(dashboard.forecast_cents)}}</b></div><div class="mt-2 h-3 rounded-full bg-slate-100"><div class="h-full rounded-full bg-violet-500" :style="{width:`${Math.min(100,pct(dashboard.forecast_cents,dashboard.target_cents))}%`}"/></div></div><div class="rounded-xl bg-slate-50 p-4"><span class="text-xs text-slate-500">Meta</span><strong class="block text-xl">{{metricValue(dashboard.target_cents)}}</strong></div></div></article>
    </section>
    <section v-if="dashboardTab==='actions'" class="mt-4 grid gap-4 lg:grid-cols-2">
      <article v-for="action in nextActions" :key="action.title" class="flex gap-4 rounded-2xl border border-n-weak bg-white p-5 shadow-sm"><span class="grid size-11 shrink-0 place-content-center rounded-xl bg-blue-50 text-blue-600"><i :class="action.icon" class="size-5"/></span><div><h3 class="font-bold">{{action.title}}</h3><p class="mt-1 text-sm text-n-slate-9">{{action.detail}}</p></div></article>
    </section>
    <div class="mt-4 rounded-2xl border border-blue-100 bg-blue-50 p-4 text-sm"><b>💡 Dica do NICO</b> — O forecast e o realizado desta tela são calculados com os dados reais do CRM: pedidos/vendas e negócios em aberto.</div>
  </template>

  <template v-else>
    <header class="mb-4 flex items-center justify-between"><div><h2 class="text-2xl font-bold">Nova Meta</h2><p class="text-sm text-n-slate-9">Crie uma meta e conecte-a aos dados reais de vendas do CRM.</p></div><button class="rounded-xl border border-n-weak bg-white px-4 py-2" @click="view='dashboard'">Cancelar</button></header>
    <div class="mb-5 grid grid-cols-5 rounded-2xl border border-n-weak bg-white p-2"><button v-for="(s,i) in ['Dados principais','Abrangência e distribuição','Valores e critérios','Produtos e indicadores','Revisão e publicação']" :key="s" class="rounded-xl px-2 py-3 text-sm" :class="step===i+1?'bg-blue-50 font-semibold text-blue-600':''" @click="step=i+1"><span class="mr-2 inline-grid size-7 place-content-center rounded-full" :class="step>=i+1?'bg-blue-600 text-white':'bg-n-alpha-2'">{{i+1}}</span>{{s}}</button></div>

    <section v-if="step===1" class="grid gap-4 xl:grid-cols-[1fr_330px]"><div class="space-y-4"><article class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Dados principais</h3><div class="mt-4 grid gap-4 md:grid-cols-2"><label class="text-sm">Nome da meta *<input v-model="form.name" class="mt-1 w-full rounded-lg border border-n-weak p-2.5"/></label><label class="text-sm">Descrição<textarea v-model="form.description" class="mt-1 w-full rounded-lg border border-n-weak p-2.5"/></label><label class="text-sm">Tipo de meta<select v-model="form.metric" class="mt-1 w-full rounded-lg border border-n-weak p-2.5"><option value="revenue">Receita (valor financeiro)</option><option value="mrr">MRR</option><option value="quantity">Quantidade</option><option value="customers">Novos clientes</option><option value="renewals">Renovações</option></select></label><label class="text-sm">Período<select v-model="form.period_kind" class="mt-1 w-full rounded-lg border border-n-weak p-2.5"><option value="monthly">Mensal</option><option value="quarterly">Trimestral</option><option value="annual">Anual</option></select></label><label class="text-sm">Data inicial<input v-model="form.period_start" type="date" class="mt-1 w-full rounded-lg border border-n-weak p-2.5"/></label><label class="text-sm">Data final<input v-model="form.period_end" type="date" class="mt-1 w-full rounded-lg border border-n-weak p-2.5"/></label></div></article><article class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Abrangência</h3><div class="mt-4 grid grid-cols-2 gap-2 md:grid-cols-5"><button v-for="x in [['company','Empresa inteira'],['team','Equipe'],['user','Usuários específicos'],['product','Produto'],['business_unit','Unidade/filial']]" :key="x[0]" class="rounded-xl border p-3 text-sm" :class="form.scope_kind===x[0]?'border-blue-500 bg-blue-50 text-blue-700':'border-n-weak'" @click="form.scope_kind=x[0]">{{x[1]}}</button></div></article><article class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Valores e critérios</h3><div class="mt-4 grid gap-4 md:grid-cols-3"><label class="text-sm">Valor da meta (R$)<input v-model="form.target" type="number" class="mt-1 w-full rounded-lg border border-n-weak p-2.5"/></label><label class="text-sm">Forma de cálculo<select v-model="form.calculation_method" class="mt-1 w-full rounded-lg border border-n-weak p-2.5"><option value="approved_orders">Vendas/pedidos aprovados</option><option value="invoiced_orders">Pedidos faturados</option><option value="won_deals">Negócios ganhos</option></select></label><label class="mt-6 flex items-center gap-2 text-sm"><input v-model="form.settings.compare_previous" type="checkbox"/> Comparar com período anterior</label></div></article></div><aside class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Resumo da meta</h3><dl class="mt-4 space-y-3 text-sm"><div><dt class="text-n-slate-9">Nome</dt><dd class="font-semibold">{{form.name}}</dd></div><div><dt class="text-n-slate-9">Período</dt><dd>{{form.period_start}} a {{form.period_end}}</dd></div><div><dt class="text-n-slate-9">Valor</dt><dd class="text-lg font-bold">{{money(totalTarget)}}</dd></div><div><dt class="text-n-slate-9">Status</dt><dd>Rascunho</dd></div></dl></aside></section>

    <section v-if="step===2" class="grid gap-4 xl:grid-cols-[1fr_350px]"><article class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Abrangência e distribuição</h3><div class="mt-4 flex gap-2"><button class="rounded-xl bg-blue-600 px-4 py-2 text-sm text-white" @click="distributeEqually">Distribuir igualmente</button><button class="rounded-xl border border-n-weak px-4 py-2 text-sm" @click="seedAllocations">Distribuição manual</button></div><table class="mt-4 w-full text-sm"><thead><tr class="text-left text-xs text-n-slate-9"><th>Vendedor</th><th>Meta (R$)</th><th>% da meta</th><th>Peso</th></tr></thead><tbody><tr v-for="a in form.allocations" :key="a.user_id" class="border-t border-n-weak"><td class="py-3 font-semibold">{{a.name}}</td><td><input v-model="a.target" type="number" class="w-36 rounded-lg border border-n-weak p-2"/></td><td>{{pct(cents(a.target),totalTarget)}}%</td><td><input v-model="a.weight" type="number" class="w-20 rounded-lg border border-n-weak p-2"/>%</td></tr></tbody><tfoot><tr class="border-t font-bold"><td class="py-3">Total distribuído</td><td>{{money(allocationTotal)}}</td><td>{{pct(allocationTotal,totalTarget)}}%</td></tr></tfoot></table></article><aside class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Resumo da distribuição</h3><div class="mt-5 text-center"><strong class="text-3xl">{{money(totalTarget)}}</strong><p class="text-sm text-n-slate-9">Meta total</p></div><div class="mt-5"><div v-for="a in form.allocations" :key="a.user_id" class="mb-3"><div class="flex justify-between text-xs"><span>{{a.name}}</span><b>{{pct(cents(a.target),totalTarget)}}%</b></div><div class="mt-1 h-2 rounded bg-n-alpha-3"><div class="h-full rounded bg-blue-600" :style="{width:`${pct(cents(a.target),totalTarget)}%`}"/></div></div></div></aside></section>

    <section v-if="step===3" class="grid gap-4 xl:grid-cols-[1fr_330px]"><div class="space-y-4"><article class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Valores e cálculo</h3><div class="mt-4 grid gap-4 md:grid-cols-3"><label class="text-sm">{{ isMoneyMetric ? 'Valor da meta (R$)' : 'Meta (quantidade / percentual)' }}<input v-model="form.target" type="number" class="mt-1 w-full rounded-lg border border-n-weak p-2.5"/></label><label class="text-sm">Forma de cálculo<select v-model="form.calculation_method" class="mt-1 w-full rounded-lg border border-n-weak p-2.5"><option value="approved_orders">Vendas/pedidos aprovados</option><option value="invoiced_orders">Pedidos faturados</option><option value="won_deals">Negócios ganhos</option></select></label><label class="mt-6 flex gap-2"><input v-model="form.settings.compare_previous" type="checkbox"/> Comparar período anterior</label></div></article><article class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Critérios de consideração</h3><div class="mt-4 grid gap-3 md:grid-cols-2"><label class="flex gap-2"><input v-model="form.settings.include_cancellations" type="checkbox"/> Descontar cancelamentos do realizado</label><label class="flex gap-2"><input v-model="form.settings.notify" type="checkbox"/> Notificar responsáveis</label><label class="flex gap-2"><input v-model="form.settings.mandatory" type="checkbox"/> Meta obrigatória</label><label class="flex gap-2"><input v-model="form.settings.compare_previous" type="checkbox"/> Comparar com histórico</label></div></article><article class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Ritmo da meta</h3><div class="mt-4 grid grid-cols-3 gap-3"><button v-for="x in [['linear','Linear'],['growing','Crescente'],['seasonal','Sazonal']]" :key="x[0]" class="rounded-xl border p-4" :class="form.settings.rhythm===x[0]?'border-blue-500 bg-blue-50':''" @click="form.settings.rhythm=x[0]">{{x[1]}}</button></div></article></div><aside class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Resumo da meta</h3><p class="mt-4 text-sm">{{form.name}}</p><strong class="mt-2 block text-2xl">{{money(totalTarget)}}</strong><p class="mt-5 text-sm text-n-slate-9">O realizado será calculado automaticamente a partir dos pedidos/vendas do CRM.</p></aside></section>

    <section v-if="step===4" class="grid gap-4 xl:grid-cols-[1fr_350px]"><div class="space-y-4"><article class="rounded-2xl border border-n-weak bg-white p-5"><div class="flex justify-between"><h3 class="font-bold">Produtos e categorias</h3><button class="rounded-lg border border-n-weak px-3 py-2 text-sm" @click="distributeProducts">Distribuir igualmente</button></div><table class="mt-4 w-full text-sm"><thead><tr class="text-left text-xs text-n-slate-9"><th>Produto</th><th>Meta (R$)</th><th>% da meta</th></tr></thead><tbody><tr v-for="p in form.product_targets" :key="p.product_id" class="border-t"><td class="py-3 font-semibold">{{p.name}}</td><td><input v-model="p.target" type="number" class="w-40 rounded-lg border border-n-weak p-2"/></td><td>{{pct(cents(p.target),totalTarget)}}%</td></tr></tbody><tfoot><tr class="border-t font-bold"><td class="py-3">Total dos produtos</td><td>{{money(productTotal)}}</td><td>{{pct(productTotal,totalTarget)}}%</td></tr></tfoot></table></article><article class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Indicadores adicionais</h3><table class="mt-4 w-full text-sm"><thead><tr><th class="text-left">Indicador</th><th>Tipo</th><th>Meta</th><th>Peso</th></tr></thead><tbody><tr v-for="i in form.indicators" :key="i.name" class="border-t"><td class="py-3 font-semibold">{{i.name}}</td><td class="text-center">{{i.kind}}</td><td><input v-model="i.target" class="w-32 rounded border p-2"/></td><td><input v-model="i.weight" class="w-20 rounded border p-2"/>%</td></tr></tbody></table></article></div><aside class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Distribuição por produto</h3><div v-for="p in form.product_targets" :key="p.product_id" class="mt-4"><div class="flex justify-between text-sm"><span>{{p.name}}</span><b>{{pct(cents(p.target),totalTarget)}}%</b></div><div class="mt-1 h-2 rounded bg-n-alpha-3"><div class="h-full rounded bg-emerald-500" :style="{width:`${pct(cents(p.target),totalTarget)}%`}"/></div></div></aside></section>

    <section v-if="step===5" class="grid gap-4 xl:grid-cols-[1fr_330px]"><div class="space-y-4"><article class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Resumo da meta</h3><div class="mt-4 grid gap-3 md:grid-cols-4"><div class="rounded-xl bg-n-alpha-1 p-4"><span class="text-xs text-n-slate-9">Tipo</span><b class="block">Receita</b></div><div class="rounded-xl bg-n-alpha-1 p-4"><span class="text-xs text-n-slate-9">Período</span><b class="block">{{form.period_start}} a {{form.period_end}}</b></div><div class="rounded-xl bg-n-alpha-1 p-4"><span class="text-xs text-n-slate-9">Abrangência</span><b class="block">{{form.scope_kind}}</b></div><div class="rounded-xl bg-n-alpha-1 p-4"><span class="text-xs text-n-slate-9">Valor</span><b class="block">{{money(totalTarget)}}</b></div></div></article><article class="grid gap-4 md:grid-cols-2"><div class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Distribuição da meta</h3><div v-for="a in form.allocations" :key="a.user_id" class="mt-2 flex justify-between text-sm"><span>{{a.name}}</span><b>{{money(cents(a.target))}}</b></div></div><div class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Produtos e categorias</h3><div v-for="p in form.product_targets" :key="p.product_id" class="mt-2 flex justify-between text-sm"><span>{{p.name}}</span><b>{{money(cents(p.target))}}</b></div></div></article></div><aside class="space-y-4"><article class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Verificações da meta</h3><div class="mt-4 rounded-xl bg-emerald-50 p-4 text-sm font-semibold text-emerald-700">✓ Dados principais preenchidos</div><div class="mt-2 rounded-xl bg-emerald-50 p-4 text-sm font-semibold text-emerald-700">✓ Distribuição configurada</div><div class="mt-2 rounded-xl bg-emerald-50 p-4 text-sm font-semibold text-emerald-700">✓ Critérios definidos</div></article><article class="rounded-2xl border border-n-weak bg-white p-5"><h3 class="font-bold">Publicar meta</h3><p class="mt-2 text-sm text-n-slate-9">Ao publicar, a meta passa a ser considerada no dashboard e no acompanhamento comercial.</p><button class="mt-4 w-full rounded-xl bg-emerald-600 px-4 py-3 font-semibold text-white" :disabled="saving" @click="save('active')">Publicar meta</button></article></aside></section>

    <footer class="mt-5 flex justify-between"><button class="rounded-xl border border-n-weak bg-white px-5 py-2.5" @click="step>1?step--:view='dashboard'">← Voltar</button><div class="flex gap-2"><button class="rounded-xl border border-n-weak bg-white px-5 py-2.5" :disabled="saving" @click="save('draft')">Salvar como rascunho</button><button v-if="step<5" class="rounded-xl bg-blue-600 px-6 py-2.5 font-semibold text-white" @click="step++">Avançar →</button></div></footer>
  </template>
</div>
</template>
